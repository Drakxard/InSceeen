const test=require('node:test'),assert=require('node:assert/strict'),core=require('../modules/anotaciones/core.js');
test('starts with one blank card',()=>{const state=core.normalize(null);assert.equal(state.tarjetas.length,1);assert.equal(state.tarjetas[0].cabecera,'')});
test('advances only after a header and keeps a single trailing blank',()=>{const state=core.normalize(null);assert.equal(core.advance(state,0),0);state.tarjetas[0].cabecera='Tema';assert.equal(core.advance(state,0),1);assert.equal(state.tarjetas.length,2);assert.equal(core.normalize(state).tarjetas.length,2)});
test('deletes cards and always leaves one',()=>{const state=core.normalize(null);state.tarjetas[0].cabecera='Tema';core.advance(state,0);assert.equal(core.remove(state,0),0);assert.equal(state.tarjetas.length,1);core.remove(state,0);assert.equal(state.tarjetas.length,1)});
test('restores an undoable removal at its original position',()=>{const state=core.normalize(null);state.tarjetas[0].cabecera='Uno';core.advance(state,0);state.tarjetas[1].cabecera='Dos';core.advance(state,1);const operation=core.removeUndoable(state,1);assert.deepEqual(state.tarjetas.map(card=>card.cabecera),['Uno','']);assert.equal(core.restoreRemoved(state,operation),1);assert.deepEqual(state.tarjetas.map(card=>card.cabecera),['Uno','Dos',''])});
test('undo replaces the temporary blank created after deleting the only card',()=>{const state=core.normalize(null);state.tarjetas[0].cabecera='Única';const originalId=state.tarjetas[0].id;const operation=core.removeUndoable(state,0);assert.equal(state.tarjetas.length,1);assert.notEqual(state.tarjetas[0].id,originalId);assert.equal(core.restoreRemoved(state,operation),0);assert.equal(state.tarjetas.length,1);assert.equal(state.tarjetas[0].id,originalId)});
test('deleting and restoring the trailing blank preserves exactly one trailing blank',()=>{const state=core.normalize(null);state.tarjetas[0].cabecera='Tema';core.advance(state,0);const blankId=state.tarjetas[1].id;const operation=core.removeUndoable(state,1);assert.equal(state.tarjetas.length,2);assert.equal(state.tarjetas.at(-1).cabecera,'');assert.notEqual(state.tarjetas.at(-1).id,blankId);core.restoreRemoved(state,operation);assert.equal(state.tarjetas.length,2);assert.equal(state.tarjetas.at(-1).id,blankId)});
test('classifies dominant swipes with one shared threshold',()=>{assert.equal(core.classifySwipe(-51,0),'left');assert.equal(core.classifySwipe(51,0),'right');assert.equal(core.classifySwipe(0,-51),'up');assert.equal(core.classifySwipe(0,51),'down');assert.equal(core.classifySwipe(50,0),null);assert.equal(core.classifySwipe(60,60),null);assert.equal(core.classifySwipe(10,12),null)});
